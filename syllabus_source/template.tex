% NOTICE: This file modified by an LLM coding system on 2026-07-26.
% Pandoc LaTeX template for the CS 418 syllabus (XeLaTeX).
\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{fontspec}
\usepackage{microtype}
% [table] brings in colortbl's \rowcolors, used for the banded schedule table.
\usepackage[table]{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{calc}
\usepackage{longtable}
\usepackage{array}
\usepackage{booktabs}
\usepackage{setspace}
\usepackage[normalem]{ulem}
\usepackage{graphicx}
% Every image keeps its aspect ratio. Pandoc emits `height=\textheight` alongside
% any width it is given; older pandocs (the CI runner's, notably) do not add
% `keepaspectratio` to that, which stretched the signal drawings to the full text
% height. Setting it as a graphicx default fixes it for whatever pandoc emits.
\setkeys{Gin}{keepaspectratio}
% Figures stay where they are written (a syllabus reads top to bottom, so a
% float that drifts to the next page lands away from the text that introduces
% it), and captions carry no "Figure N:" label.
\usepackage{float}
\floatplacement{figure}{H}
\usepackage[labelformat=empty]{caption}
\usepackage{fancyhdr}
\usepackage{eso-pic}
\usepackage[explicit]{titlesec}
\usepackage{tikz}

\setmainfont{$main_font$}

\hypersetup{colorlinks=true, linkcolor=blue, urlcolor=blue, citecolor=blue}

% Word-like paragraph behavior (tighter)
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.18\baselineskip}
\setstretch{1.03}
\raggedbottom

% Lists (Word-like indentation and tight spacing)
\setlist[itemize]{leftmargin=1.25em, itemsep=0.15em, topsep=0.15em, parsep=0pt, partopsep=0pt}
\setlist[enumerate]{leftmargin=1.25em, itemsep=0.15em, topsep=0.15em, parsep=0pt, partopsep=0pt}

% Gray boxed section heading (Word-like)
\definecolor{uiclightgray}{RGB}{200,200,200}

\setcounter{secnumdepth}{-1}
\titleformat{\section}
  {\Large\bfseries\sffamily}
  {}
  {0em}
  {%
    \begin{tikzpicture}[baseline=(title.base)]
      \node[
        inner sep=8pt,
        fill=uiclightgray,
        draw=black,
        line width=0.8pt,
        text width=\dimexpr\textwidth-16pt\relax,
        align=left,
        anchor=west
      ] (title) {#1};
    \end{tikzpicture}%
  }
  []
\titlespacing*{\section}{0pt}{0.9\baselineskip}{0.6\baselineskip}

% Subsections: compact and bold (no bar)
\titleformat{\subsection}{\bfseries}{}{0em}{#1}
\titlespacing*{\subsection}{0pt}{0.8\baselineskip}{0.35\baselineskip}

% Signal aspects: the drawing sits beside the rule it stands for, the way you
% read a signal trackside — aspect first, then what it tells you to do. Two
% top-aligned minipages stand in for the HTML flexbox.
\newlength{\signalwidth}
\setlength{\signalwidth}{1.1in}
\newcommand{\signalaspectstart}{%
  \par\medskip\noindent
  \begin{minipage}[t]{\signalwidth}\vspace{0pt}}
\newcommand{\signalaspectmid}{%
  \end{minipage}\hfill
  \begin{minipage}[t]{\dimexpr\textwidth-\signalwidth-1.2em\relax}\vspace{0pt}}
\newcommand{\signalaspectend}{\end{minipage}\par\medskip}

% Tables. The schedule table alternates white and a very light gray, switched
% on around that table alone by schedule.lua.
\definecolor{schedulerow}{RGB}{245,245,245}
\setlength{\LTpre}{0pt}
\setlength{\LTpost}{0pt}
\renewcommand{\arraystretch}{1.02}
\setlength{\tabcolsep}{6pt}

% Pandoc shims
\newcounter{none}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\real}[1]{#1}
\providecommand{\pandocbounded}[1]{#1}

% Running header: logo left, build stamp and HTML link right.
\definecolor{headergray}{RGB}{190,190,190}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\setlength{\headheight}{26pt}
\fancyhead[L]{\includegraphics[height=18pt]{$logo_pdf$}}
\fancyhead[R]{%
  \raisebox{4pt}[0pt][0pt]{%
    {\fontsize{6.5}{8}\selectfont\color{headergray}%
    This version created $created$ \quad
    \begingroup
      \hypersetup{urlcolor=headergray}
      \href{https://dodatascience.fun/syllabus/}{\uline{Click here for HTML version}}%
    \endgroup}%
  }%
}

\begin{document}

\AddToShipoutPictureBG{%
  \AtPageCenter{%
    \makebox(0,0){%
      \rotatebox{45}{%
        \textcolor{black!10}{%
          \bfseries\fontsize{54}{54}\selectfont WORK IN PROGRESS%
        }%
      }%
    }%
  }%
}

\begin{center}
  {\footnotesize\color{headergray}\textbf{WORK IN PROGRESS} --- draft syllabus subject to revision}\par
  \vspace{0.75em}
  {\Large\bfseries\sffamily $course-title$}\par
  \vspace{0.5em}
  \textbf{$college$}, $credit-hours$\par
\end{center}

\vspace{0.5\baselineskip}

$body$

\end{document}
