# Asset Location

Do not add assets to this directory.

Place assets in the parent docs directory at
`../../assets/` (`docs/assets/`) instead. This allows the chapters and slides to reference and reuse the same images.